// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: guest/v2/channel.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "grok.desktop.guest.v2";

export enum ChannelDirection {
  CHANNEL_DIRECTION_UNSPECIFIED = 0,
  CHANNEL_DIRECTION_HOST_TO_GUEST = 1,
  CHANNEL_DIRECTION_GUEST_TO_HOST = 2,
  UNRECOGNIZED = -1,
}

/**
 * ProvisionChannel is delivered once over a LocalSystem-only Hyper-V socket.
 * The channel key and nonces are generated with the operating-system CSPRNG and
 * exist only for one VM boot. They are never persisted or placed in HCS JSON.
 */
export interface ProvisionChannel {
  protocolVersion: number;
  bootId: Buffer;
  channelKey: Buffer;
  hostNonce: Buffer;
}

/** ProvisionChannelAck proves the guest received the ephemeral channel key. */
export interface ProvisionChannelAck {
  protocolVersion: number;
  bootId: Buffer;
  guestNonce: Buffer;
  hmacSha256: Buffer;
}

/**
 * ProvisionChannelProof is the deterministic protobuf body authenticated by
 * ProvisionChannelAck. It deliberately excludes the channel key and the proof
 * itself while binding both independently generated nonces to the boot.
 */
export interface ProvisionChannelProof {
  protocolVersion: number;
  bootId: Buffer;
  hostNonce: Buffer;
  guestNonce: Buffer;
}

/**
 * AuthenticatedPayload deliberately contains no maps. Implementations use
 * deterministic protobuf serialization and a protocol-domain prefix as the MAC
 * input. control_frame is the exact bounded guest-control payload during the v2
 * migration; it is authenticated before JSON decoding.
 */
export interface AuthenticatedPayload {
  protocolVersion: number;
  bootId: Buffer;
  direction: ChannelDirection;
  sequence: bigint;
  requestId: string;
  deadlineUnixMs: bigint;
  controlFrame: Buffer;
}

export interface AuthenticatedFrame {
  payload: AuthenticatedPayload | undefined;
  hmacSha256: Buffer;
}

function createBaseProvisionChannel(): ProvisionChannel {
  return { protocolVersion: 0, bootId: Buffer.alloc(0), channelKey: Buffer.alloc(0), hostNonce: Buffer.alloc(0) };
}

export const ProvisionChannel: MessageFns<ProvisionChannel> = {
  encode(message: ProvisionChannel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== 0) {
      writer.uint32(8).uint32(message.protocolVersion);
    }
    if (message.bootId.length !== 0) {
      writer.uint32(18).bytes(message.bootId);
    }
    if (message.channelKey.length !== 0) {
      writer.uint32(26).bytes(message.channelKey);
    }
    if (message.hostNonce.length !== 0) {
      writer.uint32(34).bytes(message.hostNonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProvisionChannel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProvisionChannel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bootId = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.channelKey = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.hostNonce = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ProvisionChannel>): ProvisionChannel {
    return ProvisionChannel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProvisionChannel>): ProvisionChannel {
    const message = createBaseProvisionChannel();
    message.protocolVersion = object.protocolVersion ?? 0;
    message.bootId = object.bootId ?? Buffer.alloc(0);
    message.channelKey = object.channelKey ?? Buffer.alloc(0);
    message.hostNonce = object.hostNonce ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseProvisionChannelAck(): ProvisionChannelAck {
  return { protocolVersion: 0, bootId: Buffer.alloc(0), guestNonce: Buffer.alloc(0), hmacSha256: Buffer.alloc(0) };
}

export const ProvisionChannelAck: MessageFns<ProvisionChannelAck> = {
  encode(message: ProvisionChannelAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== 0) {
      writer.uint32(8).uint32(message.protocolVersion);
    }
    if (message.bootId.length !== 0) {
      writer.uint32(18).bytes(message.bootId);
    }
    if (message.guestNonce.length !== 0) {
      writer.uint32(26).bytes(message.guestNonce);
    }
    if (message.hmacSha256.length !== 0) {
      writer.uint32(34).bytes(message.hmacSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProvisionChannelAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProvisionChannelAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bootId = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.guestNonce = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.hmacSha256 = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ProvisionChannelAck>): ProvisionChannelAck {
    return ProvisionChannelAck.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProvisionChannelAck>): ProvisionChannelAck {
    const message = createBaseProvisionChannelAck();
    message.protocolVersion = object.protocolVersion ?? 0;
    message.bootId = object.bootId ?? Buffer.alloc(0);
    message.guestNonce = object.guestNonce ?? Buffer.alloc(0);
    message.hmacSha256 = object.hmacSha256 ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseProvisionChannelProof(): ProvisionChannelProof {
  return { protocolVersion: 0, bootId: Buffer.alloc(0), hostNonce: Buffer.alloc(0), guestNonce: Buffer.alloc(0) };
}

export const ProvisionChannelProof: MessageFns<ProvisionChannelProof> = {
  encode(message: ProvisionChannelProof, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== 0) {
      writer.uint32(8).uint32(message.protocolVersion);
    }
    if (message.bootId.length !== 0) {
      writer.uint32(18).bytes(message.bootId);
    }
    if (message.hostNonce.length !== 0) {
      writer.uint32(26).bytes(message.hostNonce);
    }
    if (message.guestNonce.length !== 0) {
      writer.uint32(34).bytes(message.guestNonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProvisionChannelProof {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProvisionChannelProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bootId = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.hostNonce = Buffer.from(reader.bytes());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.guestNonce = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ProvisionChannelProof>): ProvisionChannelProof {
    return ProvisionChannelProof.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProvisionChannelProof>): ProvisionChannelProof {
    const message = createBaseProvisionChannelProof();
    message.protocolVersion = object.protocolVersion ?? 0;
    message.bootId = object.bootId ?? Buffer.alloc(0);
    message.hostNonce = object.hostNonce ?? Buffer.alloc(0);
    message.guestNonce = object.guestNonce ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseAuthenticatedPayload(): AuthenticatedPayload {
  return {
    protocolVersion: 0,
    bootId: Buffer.alloc(0),
    direction: 0,
    sequence: 0n,
    requestId: "",
    deadlineUnixMs: 0n,
    controlFrame: Buffer.alloc(0),
  };
}

export const AuthenticatedPayload: MessageFns<AuthenticatedPayload> = {
  encode(message: AuthenticatedPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== 0) {
      writer.uint32(8).uint32(message.protocolVersion);
    }
    if (message.bootId.length !== 0) {
      writer.uint32(18).bytes(message.bootId);
    }
    if (message.direction !== 0) {
      writer.uint32(24).int32(message.direction);
    }
    if (message.sequence !== 0n) {
      if (BigInt.asUintN(64, message.sequence) !== message.sequence) {
        throw new globalThis.Error("value provided for field message.sequence of type uint64 too large");
      }
      writer.uint32(32).uint64(message.sequence);
    }
    if (message.requestId !== "") {
      writer.uint32(42).string(message.requestId);
    }
    if (message.deadlineUnixMs !== 0n) {
      if (BigInt.asUintN(64, message.deadlineUnixMs) !== message.deadlineUnixMs) {
        throw new globalThis.Error("value provided for field message.deadlineUnixMs of type uint64 too large");
      }
      writer.uint32(48).uint64(message.deadlineUnixMs);
    }
    if (message.controlFrame.length !== 0) {
      writer.uint32(58).bytes(message.controlFrame);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticatedPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticatedPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bootId = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.direction = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sequence = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.requestId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.deadlineUnixMs = reader.uint64() as bigint;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.controlFrame = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<AuthenticatedPayload>): AuthenticatedPayload {
    return AuthenticatedPayload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticatedPayload>): AuthenticatedPayload {
    const message = createBaseAuthenticatedPayload();
    message.protocolVersion = object.protocolVersion ?? 0;
    message.bootId = object.bootId ?? Buffer.alloc(0);
    message.direction = object.direction ?? 0;
    message.sequence = (object.sequence !== undefined && object.sequence !== null) ? BigInt(object.sequence) : 0n;
    message.requestId = object.requestId ?? "";
    message.deadlineUnixMs = (object.deadlineUnixMs !== undefined && object.deadlineUnixMs !== null)
      ? BigInt(object.deadlineUnixMs)
      : 0n;
    message.controlFrame = object.controlFrame ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseAuthenticatedFrame(): AuthenticatedFrame {
  return { payload: undefined, hmacSha256: Buffer.alloc(0) };
}

export const AuthenticatedFrame: MessageFns<AuthenticatedFrame> = {
  encode(message: AuthenticatedFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload !== undefined) {
      AuthenticatedPayload.encode(message.payload, writer.uint32(10).fork()).join();
    }
    if (message.hmacSha256.length !== 0) {
      writer.uint32(18).bytes(message.hmacSha256);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticatedFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticatedFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = AuthenticatedPayload.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hmacSha256 = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<AuthenticatedFrame>): AuthenticatedFrame {
    return AuthenticatedFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticatedFrame>): AuthenticatedFrame {
    const message = createBaseAuthenticatedFrame();
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? AuthenticatedPayload.fromPartial(object.payload)
      : undefined;
    message.hmacSha256 = object.hmacSha256 ?? Buffer.alloc(0);
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
